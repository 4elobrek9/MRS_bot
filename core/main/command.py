from aiogram import types
from aiogram.enums import ParseMode
from aiogram.utils.markdown import hbold

async def cmd_help(message: types.Message):
    """Обновленная справка по командам с RPG системой"""
    help_text = (
        "🤖 **Доступные команды бота** 🤖\n\n"
        "⚡️ **Все команды можно использовать двумя способами:**\n"
        "1. **Через `/`** (напр., `/profile`)\n"
        "2. **Обычным текстом** (напр., `профиль`)\n\n"

        "🎒 **RPG Система:**\n"
        "• `/profile` (`профиль`) - Ваш игровой профиль\n"
        "• `/inventory` (`инвентарь`) - Ваш инвентарь и фоны\n"
        "• `/work` (`работать`) - Выполнить работу (заработок LUM)\n"
        "• `/top` (`топ`) - Топ игроков по уровню\n"
        "• `/shop` (`магазин`) - Магазин предметов и фонов\n"
        "• `/pshop` (`пмагазин`) - Магазин за PLUM-коины\n"
        "• `/quests` (`задания`/`квесты`) - Ежедневные и еженедельные задания\n\n"

        "💸 **Экономика и Переводы:**\n"
        f"• `/give [сумма] [ник]` ({hbold('дать')}) - Передать LUM пользователю (напр., /give 100 @username) (Текстовая команда для переводов не рекомендуется)\n"
        "• `/invest` (`инвестировать`) - Инвестировать LUM под проценты\n"
        "• `/auction` (`аукцион`) - Активные аукционы\n"
        "• `/market` (`рынок`) - Рынок для торговли с игроками\n\n"

        "❤️ **RP/Здоровье:**\n"
        "• `/myhp` (`мое здоровье`) - Проверить текущее HP\n"
        "• `/heal` (`лечить`) - Использовать аптечку\n"
        "• `/rpactions` (`рп действия`) - Список доступных RP действий\n\n"

        "⚙️ **Общие:**\n"
        "• `/joke` (`анекдот`) - Случайный анекдот\n"
        "• `/stats` (`статистика`) - Статистика использования\n"
        "• `/help` - Эта справка\n"
    )

    await message.reply(help_text, parse_mode=ParseMode.MARKDOWN)
